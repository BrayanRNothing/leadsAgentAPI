require('dotenv').config(); const { GoogleGenerativeAI } = require('@google/generative-ai'); const genAI = new GoogleGenerativeAI(process.env.GOOGLE_API_KEY); const model = genAI.getGenerativeModel({ model: 'gemini-1.5-flash' }); model.generateContent('Dime 3 sinónimos de taller mecanico separados por comas').then(res => console.log('Exito:', res.response.text())).catch(err => console.error('Error:', err.message));
